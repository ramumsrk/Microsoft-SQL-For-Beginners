SELECT -- Projection
       *
  FROM HumanResources.vEmployee;

SELECT -- Projection
       FirstName,
       LastName,
       EmailAddress,
       PhoneNumber
  FROM Sales.vIndividualCustomer;                 